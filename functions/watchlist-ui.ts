// Static assets are bundled into the Function; no third-party scripts or tracking.
export const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#173f35"><title>Finder · Your pressing watchlist</title><link rel="manifest" href="/manifest.webmanifest"><meta name="apple-mobile-web-app-capable" content="yes"><link rel="stylesheet" href="/app.css"><script src="/app.js" defer></script></head><body>
<header><a class="brand" href="/" aria-label="Finder home"><span class="record">◉</span> Finder</a><span class="private">PRIVATE PRESSING HUNTER</span><div id="account" hidden><button id="notifications" class="quiet">Enable notifications</button><button id="signout" class="quiet">Sign out</button></div></header>
<main><div id="notice" role="status" aria-live="polite"></div>
<section id="signin" class="login" hidden><span class="eyebrow">YOUR NEXT FIND</span><h1>The pressing you want.<br>A price you choose.</h1><p>Keep an eye on specific vinyl pressings. Review new listings with the evidence that matters.</p><form id="login"><label>Email<input id="email" type="email" autocomplete="email" required placeholder="Your approved email"></label><label id="codefield" hidden>Verification code<input id="code" autocomplete="one-time-code" inputmode="numeric" pattern="[0-9]{6,8}"></label><button type="submit" id="loginbutton">Send sign-in code</button></form><p class="small">Private access only. No password to remember.</p></section>
<section id="dashboard" hidden><div class="intro"><div><span class="eyebrow">YOUR COLLECTION, NEXT</span><h1>Watch the right pressing.</h1><p>Likely matches, the signs that matter, and prices you set.</p></div><button id="add">+ Add a pressing</button></div>
<div class="info">Every lead needs your review. Seller claims are unverified. Prices show item + shipping where known, excluding tax and duties.</div>
<section class="operations" aria-labelledby="operationsheading"><h2 id="operationsheading">Monitoring health</h2><div id="operations"></div><details><summary>Recent scan history</summary><div id="runhistory"></div></details><p id="pushstatus" class="small"></p><div class="actions"><button id="testpush" class="quiet">Send test notification</button></div><p class="small">On iPhone: open Finder in Safari, use Share → Add to Home Screen, then open Finder from that icon and enable notifications. A test is separate from pressing alerts.</p></section>
<section class="operations" aria-labelledby="accuracyheading"><h2 id="accuracyheading">Your verdicts</h2><div id="accuracy"></div></section>
<section aria-labelledby="watchheading"><div class="sectionhead"><h2 id="watchheading">Your watches <span id="watchcount"></span></h2><span class="small">Scheduled scans can be delayed · check each last-success time</span></div><div id="watches" class="watches"></div></section>
<section id="editor" class="editor" hidden><h2 id="editheading">Add a pressing</h2><form id="watchform"><div class="fields"><label>Discogs release link or ID<input name="release_id" required placeholder="https://www.discogs.com/release/…"></label><label>Your label <span class="muted">optional</span><input name="label" maxlength="120" placeholder="The version I’m looking for"></label><label>Maximum when it’s likely yours <span class="muted">item + shipping · optional</span><input name="maximum_subtotal" type="number" min="0.01" max="9999999" step="0.01" placeholder="Collect leads at any price"></label><label>Maximum for unclear listings <span class="muted">optional · the most you’d gamble</span><input name="gamble_max" type="number" min="0.01" max="9999999" step="0.01" placeholder="No alerts for unclear listings"></label><label>Auction alerts<select name="auction_alert_minutes"><option value="120">2 hours before the end</option><option value="30">30 minutes before</option><option value="60">1 hour before</option><option value="360">6 hours before</option><option value="1440">24 hours before</option><option value="0">Off</option></select></label><label>Alerts<select name="alert_mode"><option value="review_leads">Review leads · uncertainty included</option><option value="strict">Strict · complete comparison required</option></select></label><label>Currency<select name="currency"><option>USD</option><option>GBP</option><option>EUR</option><option>CAD</option><option>AUD</option></select></label><label>Seller condition<select name="condition"><option value="any">Any · verify media and sleeve</option><option value="new">New only</option><option value="used">Used only</option></select></label><label>Delivery country <span class="muted">two-letter code</span><input name="country" maxlength="2" pattern="[A-Za-z]{2}" placeholder="US"></label><label>Delivery postal code<input name="postal_code" maxlength="16" placeholder="For shipping estimates"></label><label>Extra searches <span class="muted">optional · one per line, up to 2 (misspellings, “lot”)</span><textarea name="extra_queries" rows="2" maxlength="210"></textarea></label></div><div class="sheet"><h3>Cheat sheet</h3><p class="small">Signs of your pressing: a listing that mentions every <strong>required</strong> sign moves to “Likely yours”. Signs of a common version move a listing to “Likely another version”. Finder reads the seller’s text only, so always check the photos.</p><div id="tells"></div><button type="button" class="quiet" data-addsign="tells">+ Sign of my pressing</button><div id="anti_tells"></div><button type="button" class="quiet" data-addsign="anti_tells">+ Sign of a common version</button></div><p class="small">A price ceiling needs a delivery destination. Record and sleeve grades still need checking in the listing. Discovery currently searches eBay US.</p><div class="actions"><button type="submit">Save watch</button><button type="button" id="cancel" class="quiet">Cancel</button></div></form></section>
<section aria-labelledby="inboxheading"><div class="sectionhead"><h2 id="inboxheading">Review inbox <span id="leadcount"></span></h2><button id="refresh" class="quiet">Refresh</button></div><div class="filters" role="group" aria-label="Filter leads"><button data-filter="review" class="selected">To review</button><button data-filter="possible_pressing">Likely yours</button><button data-filter="family_review">Unclear · check photos</button><button data-filter="conflicting">Likely another version</button><button data-filter="unrelated">Other search results</button><button data-filter="judged">Judged</button><button data-filter="unavailable">Unavailable on recheck</button><button data-filter="dismissed">Dismissed</button></div><label>Price view<select id="prices"><option value="watch">Within each watch’s ceiling</option><option value="all">All prices · includes unknown totals</option></select></label><p class="small">Watches without a ceiling show all prices. With a ceiling, only fresh totals quoted for your destination qualify; auctions count their current bid. Use All prices to see higher prices, unknown totals and older references. Discovery keeps tracking them. Judged shows all prices and older references regardless of this setting.</p><div id="leads"></div><div class="actions"><button id="firstpage" class="quiet">First page</button><button id="nextpage" class="quiet">Next page</button></div><p class="small">Bounded searches may miss listings. An empty inbox does not mean no copies are for sale. Older references stay visible; provider content older than six hours is withheld until refreshed.</p></section>
</section><footer><span>Built for a careful second look.</span><a href="https://www.discogs.com" target="_blank" rel="noreferrer">Data provided by Discogs</a></footer></main></body></html>`;

export const stylesheet = `:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;color:#203b34;background:#f6f5f0;font-synthesis:none}*{box-sizing:border-box}body{margin:0}header{display:flex;align-items:center;gap:22px;padding:22px 5%;border-bottom:1px solid #dcded5;background:#fbfaf7}.brand{color:#173f35;text-decoration:none;font-weight:800;font-size:27px;letter-spacing:-1px}.record{font-size:32px;vertical-align:-2px;margin-right:7px}.private,.eyebrow{font-size:11px;font-weight:750;letter-spacing:1.6px;color:#65796e}#account{margin-left:auto}main{max-width:1180px;margin:auto;padding:42px 28px}h1{font-size:clamp(32px,4vw,49px);letter-spacing:-1.9px;line-height:1.13;margin:12px 0 15px}h2{font-size:20px;letter-spacing:-.5px;margin:0}h3{font-size:18px;margin:0 0 10px}p{line-height:1.6;color:#66756d}a{color:#215c48}button{background:#204f40;color:#fff;border:1px solid #204f40;border-radius:8px;padding:11px 17px;font:inherit;font-size:13px;font-weight:650;cursor:pointer}button:hover{filter:brightness(.94)}button:disabled{opacity:.55;cursor:wait}.quiet{background:transparent;border-color:#d2d8ce;color:#365d4b}.intro{display:flex;align-items:center;justify-content:space-between;gap:30px}.intro p{margin-bottom:25px}.info{background:#e9eee3;border:1px solid #dce4d3;padding:14px 18px;border-radius:8px;color:#4f6657;font-size:13px;line-height:1.5}.sectionhead{display:flex;gap:15px;justify-content:space-between;align-items:center;margin:36px 0 18px}.small,.muted{font-size:12px;color:#768074}.operations{margin-top:22px;padding:20px;border:1px solid #dedfd5;border-radius:12px;background:#fffefb}.operations p{margin:9px 0;font-size:13px}.operations summary{cursor:pointer;font-size:13px}.runrow{padding:9px 0;border-bottom:1px solid #e5e7df;font-size:12px;line-height:1.6}.watches{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:15px}.watch,.lead,.editor{background:#fffefb;border:1px solid #dedfd5;border-radius:12px;padding:23px}.watch{display:flex;flex-direction:column;gap:9px}.watch p{margin:0;font-size:13px}.watch .actions{margin-top:auto;padding-top:9px}.actions{display:flex;gap:9px;flex-wrap:wrap}.actions button{padding:8px 11px}.badge{display:inline-block;border-radius:5px;font-size:11px;font-weight:700;padding:5px 8px;background:#e8efdf;color:#436443;width:fit-content}.amber{background:#fff0d8;color:#86632c}.red{background:#f7e5df;color:#955342}.filters{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:16px}.filters button{background:transparent;border-color:#d9ddd2;color:#64745f}.filters .selected{background:#244e3f;border-color:#244e3f;color:white}.lead{display:grid;grid-template-columns:minmax(0,1fr) 180px;gap:25px;margin:12px 0}.lead h3{font-size:17px;line-height:1.5}.lead p{margin:8px 0;font-size:13px}.price{font-size:27px;font-weight:750;letter-spacing:-1px;margin:9px 0}.lead aside{border-left:1px solid #e5e7df;padding-left:22px}.lead aside a{display:block;font-size:13px;font-weight:700;margin:17px 0}.lead details{font-size:12px;color:#647565;line-height:1.8}.lead summary{cursor:pointer}.empty{padding:42px 25px;text-align:center;border:1px dashed #cbd4c5;border-radius:10px;color:#6f7f6b}.fields{display:grid;grid-template-columns:1fr 1fr;gap:18px}.editor{margin-top:25px}.editor h2{margin-bottom:23px}label{display:flex;flex-direction:column;gap:7px;font-size:13px;font-weight:600}input,select{border:1px solid #cdd6ca;border-radius:6px;background:white;color:#203b34;padding:12px;font:inherit;min-width:0}input:focus,select:focus,button:focus-visible{outline:2px solid #81a082;outline-offset:3px}.login{max-width:550px;margin:35px auto 80px}.login form{display:grid;gap:17px;max-width:380px;margin-top:30px}.login p{max-width:460px}footer{display:flex;justify-content:space-between;margin-top:60px;padding-top:20px;border-top:1px solid #dcded5;font-size:12px;color:#788172}#notice:not(:empty){background:#fff0d8;border:1px solid #e2d4b2;border-radius:8px;padding:13px 17px;margin-bottom:25px;font-size:13px}[hidden]{display:none!important}textarea{border:1px solid #cdd6ca;border-radius:6px;background:white;color:#203b34;padding:12px;font:inherit;resize:vertical}.sheet{margin-top:22px;padding-top:18px;border-top:1px solid #e5e7df}.sheetrow{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin:8px 0}.sheetrow input:not([type=checkbox]){flex:1 1 180px}.sheet>button{margin:6px 0 14px}label.inline{flex-direction:row;align-items:center;font-weight:500}.chip{display:inline-block;border-radius:5px;font-size:11px;font-weight:700;padding:3px 7px;background:#e8efdf;color:#436443}.chip.amber{background:#fff0d8;color:#86632c}.chiprow{font-size:12px;margin:6px 0}.chiprow button,.suggest>button{padding:4px 9px;font-size:12px}.photos{display:grid;grid-template-columns:repeat(auto-fill,minmax(88px,1fr));gap:6px;margin:12px 0}.photos img{width:100%;aspect-ratio:1;object-fit:cover;border-radius:6px;border:1px solid #e5e7df;background:#f1f1ec}.verdicts{display:flex;flex-wrap:wrap;gap:5px;margin:12px 0}.verdicts button{padding:5px 8px;font-size:11px}.verdicts .selected{background:#244e3f;border-color:#244e3f;color:#fff}.message{width:100%;margin:8px 0;font-size:12px}@media(max-width:800px){.watches{grid-template-columns:1fr}.intro{align-items:start;flex-direction:column;gap:0;margin-bottom:22px}.private{display:none}.sectionhead{align-items:start;flex-direction:column}.lead{grid-template-columns:1fr}.lead aside{border:0;border-top:1px solid #e5e7df;padding:15px 0 0}.fields{grid-template-columns:1fr}main{padding:25px 18px}header{padding:16px 18px;gap:10px}#account button{font-size:11px;padding:8px}footer{gap:20px}}`;

export const javascript = String.raw`
const $=s=>document.querySelector(s);let state=null,filter='review',priceScope='watch',editing=null,codeSent=false;
const escape=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const time=s=>s?new Date(s).toLocaleString():'Not scanned yet';
const words=s=>String(s).replaceAll('_',' ');
function notice(s){$('#notice').textContent=s;}
async function api(path,data,method){const r=await fetch(path,{method:method||(data?'POST':'GET'),headers:data?{'Content-Type':'application/json'}:{},body:data?JSON.stringify(data):undefined});const value=await r.json();if(!r.ok){const e=new Error(value.error||'Request failed');e.status=r.status;throw e;}return value;}
function safeLink(s){try{const u=new URL(s);return u.protocol==='https:'&&/(^|\.)ebay\.com$/.test(u.hostname)?u.href:'#';}catch{return '#';}}
function name(w){return w.config.label||(w.catalog?[...(w.catalog.artists||[]),w.catalog.title].filter(Boolean).join(' · '):'Saved pressing');}
function coverage(w){const c=w.summary?.coverage;if(c){return 'Initial sweep: '+words(c.initial.status)+' ('+c.initial.queries_exhausted+'/'+c.initial.queries+' queries; '+c.initial.pages+' pages) · '+c.unique_retrieved+' unique listing references retrieved · '+c.pending+' awaiting details/evaluation · '+(c.outcomes.evaluated||0)+' evaluated · Latest chunk: '+(w.summary.discovery?.browse_requests||0)+' Browse requests; quota reading '+(w.summary.discovery?.quota_remaining??'unavailable')+' remaining'+(w.summary.discovery?.budget_failure?' · Budget check: '+words(w.summary.discovery.budget_failure):'')+' · Incremental: '+words(c.incremental.status)+' ('+c.incremental.queries_exhausted+'/'+c.incremental.queries+' queries; covered through '+time(c.watermarks?.every(Boolean)?c.watermarks.slice().sort()[0]:null)+') · Last complete reconciliation: '+time(c.last_reconciliation_at)+(c.partial_reason?' · Paused: '+words(c.partial_reason):'')+['initial','incremental','reconciliation'].flatMap(lane=>(c[lane].query_passes||[]).filter(p=>p.reason).map(p=>' · '+words(lane)+' query '+p.position+': '+words(p.reason))).join('')+' · Search exhaustion is for this pass, not all eBay inventory.';}const d=w.summary?.discovery;if(!d)return w.summary?.page_cap_reached?'Search cap reached · coverage is partial':'Bounded eBay discovery';const newestCount=(d.newest||[]).length;const capped=(d.newest||[]).filter(q=>q.cap_reached).length;const newest='Newest: '+newestCount+' '+(newestCount===1?'query':'queries')+' checked'+(capped?' · '+capped+' hit their page cap':'');const relevanceCount=d.initial_relevance?.length||0;const relevance=d.initial_relevance?'Relevance: '+relevanceCount+' '+(relevanceCount===1?'query':'queries')+' checked'+(d.initial_relevance.some(q=>q.cap_reached)?' · page cap reached':'')+' · ':'';const p=d.inventory_sample;const older=p?'Older: sampled query '+p.query_position+' at '+(p.returned?'results '+(p.offset+1)+'–'+(p.offset+p.returned):'offset '+p.offset+' (no results)')+(p.depth_capped?' · depth cap reached':''):'Older: inventory '+(d.inventory_due_next_scan?'due next scan':'due on a later scan');const recheck=d.candidate_recheck==='updated'||d.candidate_recheck==='new'?' · known lead refreshed':d.candidate_recheck==='unavailable'?' · known lead unavailable on recheck':'';return relevance+newest+' · '+older+recheck+' · overall coverage unmeasured';}
const KINDS=[['keyword','Word or phrase'],['color','Vinyl color'],['catalog_number','Catalog number'],['barcode','Barcode'],['label','Record label'],['country','Country'],['numbered','Numbered copy']];
const VERDICTS=[['mine','My pressing'],['other','Other version'],['unsure','Can\u2019t tell'],['bought','Bought']];
let sheet={tells:[],anti_tells:[]};
function signText(c){return c.kind==='numbered'?'Numbered':words(c.kind)+': '+c.value;}
function sheetKey(c){return c.kind+'|'+String(c.value).toLowerCase();}
function suggestions(w){const p=w.profile?.proposals;if(!p)return [];const have=new Set([...(w.config.tells||[]),...(w.config.anti_tells||[])].map(sheetKey));return [...(p.tells||[]).map(c=>({...c,list:'tells'})),...(p.anti_tells||[]).map(c=>({...c,list:'anti_tells'}))].filter(c=>!have.has(sheetKey(c)));}
function auctionText(m){m=m??120;return m===0?'off':m>=60?(m/60)+' h before the end':m+' min before the end';}
function sellerMessage(w){const signs=(w?.config.tells||[]).map(signText);return 'Hi! I\u2019m interested in this record. Could you share clear photos of both labels and of the runout (dead wax) etchings on each side?'+(signs.length?' I\u2019m looking for the pressing with: '+signs.join('; ')+'.':'')+' Thank you!';}
function cleanSheet(list){return list.map(c=>({kind:c.kind,value:c.kind==='numbered'&&!String(c.value).trim()?'numbered':String(c.value).trim(),required:!!c.required})).filter(c=>c.value);}
async function addSuggestions(w,items){const config={...w.config,tells:[...(w.config.tells||[])],anti_tells:[...(w.config.anti_tells||[])]};for(const c of items){if(config[c.list].length<12)config[c.list].push({kind:c.kind,value:c.value,required:!!c.required});}await api('/api/watches/'+w.id,config,'PUT');await load();notice('Cheat sheet updated. Listings are re-checked on the next scans.');}
function renderSheet(){for(const list of ['tells','anti_tells']){$('#'+list).innerHTML=sheet[list].map((c,i)=>'<div class="sheetrow"><select data-sheet="'+list+'|'+i+'|kind" aria-label="Sign type">'+KINDS.map(([k,l])=>'<option value="'+k+'"'+(c.kind===k?' selected':'')+'>'+l+'</option>').join('')+'</select><input data-sheet="'+list+'|'+i+'|value" value="'+escape(c.value)+'" maxlength="80" aria-label="Sign" placeholder="'+(list==='tells'?'e.g. Pink/Green':'e.g. reissue')+'">'+(list==='tells'?'<label class="inline"><input type="checkbox" data-sheet="'+list+'|'+i+'|required"'+(c.required?' checked':'')+'> Required</label>':'')+'<button type="button" class="quiet" data-removesign="'+list+'|'+i+'">Remove</button></div>').join('');}
 document.querySelectorAll('[data-sheet]').forEach(el=>{el.oninput=el.onchange=()=>{const [list,i,field]=el.dataset.sheet.split('|');sheet[list][Number(i)][field]=field==='required'?el.checked:el.value;};});
 document.querySelectorAll('[data-removesign]').forEach(b=>b.onclick=()=>{const [list,i]=b.dataset.removesign.split('|');sheet[list].splice(Number(i),1);renderSheet();});}
function renderAccuracy(){const a=state.accuracy||[];const count=(t,v)=>a.filter(x=>x.tier===t&&(!v||x.verdict===v)).reduce((s,x)=>s+Number(x.n),0);
 $('#accuracy').innerHTML=a.length?[['possible_pressing','Likely yours'],['family_review','Unclear'],['conflicting','Likely another version']].map(([t,l])=>{const n=count(t);if(!n)return '';const mine=count(t,'mine')+count(t,'bought');const other=count(t,'other');return '<p><strong>'+l+'</strong>: '+n+' judged · '+mine+' yours · '+other+' another version · '+count(t,'unsure')+' can\u2019t tell'+(mine+other?' · '+Math.round(100*mine/(mine+other))+'% yours when you could tell':'')+'</p>';}).join('')+'<p class="small">Small samples swing a lot. Judge at least 30 listings before trusting a percentage.</p>':'<p class="small">Mark listings “My pressing” or “Other version” to measure how well each tier and cheat sheet works.</p>';}
function catalogCheck(data){if(data.alternatives_checked==null)return 'Other pressings could not be compared this time.';const count=Number(data.alternatives_checked);const unresolved=Number(data.alternatives_not_ruled_out||0);return 'Compared with '+count+' other vinyl '+(count===1?'version':'versions')+((data.verify||[]).includes('catalog_alternative_search_incomplete')?' (partial list)':'')+'. '+(unresolved?unresolved+' could also fit what the seller wrote.':'None fits the seller\u2019s text as well; still check the photos.');}
function watchVerdicts(w){const rows=w.verdict_counts||[];if(!rows.length)return 'No judgments recorded yet.';const count=(tier,verdict)=>rows.filter(r=>r.tier===tier&&r.verdict===verdict).reduce((n,r)=>n+Number(r.n),0);return ['possible_pressing','family_review'].map((tier,i)=>{const yours=count(tier,'mine')+count(tier,'bought'),other=count(tier,'other');return (i?'Unclear':'Likely')+': '+yours+' yours, '+other+' other version';}).join(' · ')+'.';}
function renderOperations(){
 const o=state.operations;if(!o){$('#operations').textContent='Health history is not available.';return;}
 const c=o.counts;const warning=o.status==='attention_needed'||o.trigger_stale;
 const title=o.status==='not_measured'?'History not started':warning?'Monitoring needs attention':'Collecting reliability evidence';
 $('#operations').innerHTML='<p><span class="badge '+(warning?'amber':'')+'">'+escape(title)+'</span></p><p>'+c.completed+' completed · '+c.failed+' failed · '+c.quota_paused+' quota pauses · '+c.abandoned+' interrupted scans in the measured window.</p><p class="small">Recording since '+escape(time(o.measured_since))+' · '+Number(o.elapsed_days).toFixed(1)+' of at least 14 observation days. This does not yet certify reliability or matching accuracy.</p><p class="small">Longest recorded wait after a scan became due: '+o.max_start_delay_minutes+' min. '+o.browse_requests+' Browse request attempts recorded; other workflows also use the quota.</p><p class="small">Catch-up trigger last seen: '+escape(time(o.trigger_last_seen_at))+(o.trigger_stale?' · heartbeat missing or late':'')+'. '+o.dispatches.correlated+' dispatches linked to worker scans.</p>'+(o.history_truncated?'<p>History exceeds this view’s limit. Counts are incomplete.</p>':'');
 $('#runhistory').innerHTML=o.recent.map(r=>{const w=state.watches.find(w=>w.id===r.watch_id);return '<div class="runrow"><strong>'+escape(w?name(w):'Watch')+'</strong> · '+escape(words(r.status))+' · '+escape(time(r.started_at))+'<br>'+escape(words(r.source))+' · '+r.start_delay_minutes+' min after due'+(r.browse_requests!=null?' · '+r.browse_requests+' Browse calls':'')+(r.run_id&&/^\d+$/.test(r.run_id)?' · <a target="_blank" rel="noreferrer" href="https://github.com/nictowey/Finder/actions/runs/'+r.run_id+'">Run details ↗</a>':'')+'</div>';}).join('')||'<p>No recorded scan attempts yet.</p>';
 const n=o.notifications;$('#pushstatus').textContent=n.registered_devices+' registered notification '+(n.registered_devices===1?'device':'devices')+'. '+(n.last_delivery?'Last delivery check: '+time(n.last_delivery.at)+' · '+words(n.last_delivery.status)+'. Push-service acceptance does not prove device display.':'Device delivery has not been verified.');
}
function render(){
 renderOperations();renderAccuracy();
 $('#signin').hidden=true;$('#dashboard').hidden=false;$('#account').hidden=false;$('#watchcount').textContent='('+state.watches.length+'/'+(state.max_watches||20)+')';
 $('#watches').innerHTML=state.watches.map(w=>{const stale=!w.last_success_at||Date.now()-Date.parse(w.last_success_at)>3600000;return '<article class="watch"><span class="badge '+(stale?'amber':'')+'">'+(!w.config.enabled?'Paused':w.status==='failed'?'Scan failed · retry scheduled':w.status==='quota_paused'?'Scan paused · eBay quota needs checking':w.status==='scanning'?(Date.parse(w.lease_until)<=Date.now()?'Scan interrupted · recovery due':'Checking listings'):stale?'Waiting for a fresh scan':'Monitoring')+'</span><h3>'+escape(name(w))+'</h3><p>Likely yours: '+(w.config.maximum_subtotal?'up to '+escape(w.config.currency+' '+w.config.maximum_subtotal):'any price')+' · Unclear: '+(w.config.gamble_max?'up to '+escape(w.config.currency+' '+w.config.gamble_max):'no alerts')+'</p><p class="small">Cheat sheet: '+(w.config.tells||[]).length+' '+((w.config.tells||[]).length===1?'sign':'signs')+' of your pressing ('+(w.config.tells||[]).filter(c=>c.required).length+' required) · '+(w.config.anti_tells||[]).length+' common-version '+((w.config.anti_tells||[]).length===1?'sign':'signs')+' · Auction alerts '+escape(auctionText(w.config.auction_alert_minutes))+'</p>'+(w.profile?'<p class="small">Compared with '+Math.max(0,Number(w.profile.vinyl_versions)-1)+' other vinyl versions on Discogs'+(w.profile.partial?' (partial list)':'')+'.</p>':'')+(()=>{const sug=suggestions(w);return sug.length?'<details class="suggest"><summary>'+sug.length+' suggested '+(sug.length===1?'sign':'signs')+'</summary>'+sug.map((c,i)=>'<div class="chiprow"><span class="chip'+(c.list==='tells'?'':' amber')+'">'+(c.list==='tells'?'Yours':'Common')+'</span> '+escape(signText(c))+(c.required?' · required':'')+(c.list==='anti_tells'?' · on '+Number(c.siblings_sharing)+' other '+(Number(c.siblings_sharing)===1?'version':'versions'):' · on no other version')+' <button class="quiet" data-suggest="'+w.id+'|'+i+'">Add</button></div>').join('')+'<button class="quiet" data-suggestall="'+w.id+'">Add all</button></details>':'';})()+'<p class="small">Last success: '+escape(time(w.last_success_at))+' · Next due: '+escape(time(w.next_scan_at))+(w.config.enabled&&Date.parse(w.next_scan_at)<Date.now()?' · '+Math.floor((Date.now()-Date.parse(w.next_scan_at))/60000)+' min overdue':'')+'</p><p class="small">'+escape(coverage(w))+'</p><p class="small">In review store: '+(w.inbox_counts?.possible_pressing||0)+' likely · '+(w.inbox_counts?.family_review||0)+' unclear · '+(w.inbox_counts?.conflicting||0)+' other version · '+(w.inbox_counts?.unrelated||0)+' unrelated. Price and freshness filters may hide rows from the inbox below.</p><p class="small">Judgments: '+escape(watchVerdicts(w))+'</p>'+(w.summary?.catalog_check_failed?'<p class="small">Alternative pressing lookup failed · review available, alerts held</p>':'')+'<p class="small">Alerts: '+(w.config.alert_mode==='strict'?'strict checks':'review leads, including uncertainty')+(w.config.extra_queries?.length?' · '+w.config.extra_queries.length+' extra '+(w.config.extra_queries.length===1?'search':'searches'):'')+'</p><a class="small" target="_blank" rel="noreferrer" href="https://www.discogs.com/release/'+w.config.release_id+'">View target pressing on Discogs ↗</a><div class="actions"><button class="quiet" data-edit="'+w.id+'">Edit</button><button class="quiet" data-toggle="'+w.id+'">'+(w.config.enabled?'Pause':'Resume')+'</button><button class="quiet" data-delete="'+w.id+'">Remove</button></div></article>';}).join('')||'<div class="empty">Add a Discogs pressing to start your first watch.</div>';
 document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openEditor(b.dataset.edit));
 document.querySelectorAll('[data-suggest]').forEach(b=>b.onclick=()=>action(async()=>{const [id,i]=b.dataset.suggest.split('|');const w=state.watches.find(w=>w.id===id);await addSuggestions(w,[suggestions(w)[Number(i)]]);}));
 document.querySelectorAll('[data-suggestall]').forEach(b=>b.onclick=()=>action(async()=>{const w=state.watches.find(w=>w.id===b.dataset.suggestall);await addSuggestions(w,suggestions(w));}));
 document.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>action(async()=>{const w=state.watches.find(w=>w.id===b.dataset.toggle);await api('/api/watches/'+w.id,{...w.config,enabled:!w.config.enabled},'PUT');await load();}));
 document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{if(confirm('Remove this watch and its inbox history?'))action(async()=>{await api('/api/watches/'+b.dataset.delete,{},'DELETE');await load();});});
 const rows=state.leads.filter(r=>filter==='dismissed'?r.dismissed:filter==='judged'?!!r.verdict&&!r.dismissed:filter==='unavailable'?!r.dismissed&&r.data.availability==='unavailable_on_recheck':!r.dismissed&&r.data.availability!=='unavailable_on_recheck'&&(filter==='review'?!r.verdict&&['possible_pressing','family_review'].includes(r.data.status):r.data.status===filter));
 $('#leadcount').textContent='('+rows.length+' on this page)';$('#nextpage').disabled=!state.next_cursor;$('#firstpage').disabled=!inboxCursor;
 $('#leads').innerHTML=rows.map((r,i)=>{const w=state.watches.find(w=>w.id===r.watch_id);const stale=r.evidence_stale||Date.now()-Date.parse(r.listing.details_observed_at||r.last_seen_at)>3600000;const ended=r.listing.listing_ends_at&&Date.parse(r.listing.listing_ends_at)<Date.now();const unavailable=r.data.availability==='unavailable_on_recheck';return '<article class="lead"><div><span class="badge '+(r.data.status==='conflicting'?'red':r.data.status==='family_review'||unavailable?'amber':'')+'">'+(unavailable?'Unavailable on recheck':r.data.status==='possible_pressing'?'Likely yours · confirm in photos':r.data.status==='family_review'?'Unclear · check photos':r.data.status==='unrelated'?'Other search result':'Likely another version')+'</span><p class="small">For '+escape(w?name(w):'your watch')+'</p><h3>'+escape(r.listing.title)+'</h3><p class="small">'+escape(words(r.data.discovery_kind||'known listing'))+' · First found '+escape(time(r.first_seen_at))+(r.data.provider_started_at?' · Listed on eBay '+escape(time(r.data.provider_started_at)):'')+'</p>'+(unavailable?'<p>The item was unavailable or ended on its last direct check. The stored price may be stale; this does not show whether it sold.</p>':'')+(r.listing.images?.length?'<div class="photos">'+r.listing.images.filter(u=>/^https:\/\/i\.ebayimg\.com\//.test(u)).map((u,j)=>'<a href="'+escape(u)+'" target="_blank" rel="noreferrer"><img loading="lazy" src="'+escape(u)+'" alt="Listing photo '+(j+1)+'"></a>').join('')+'</div>':'')+(r.data.signs?.length?'<p><strong>Signs of your pressing:</strong> '+escape(r.data.signs.join(', '))+'</p>':'')+(r.data.missing_signs?.length?'<p><strong>Required but not mentioned:</strong> '+escape(r.data.missing_signs.join(', '))+'</p>':'')+(r.data.common_signs?.length?'<p><strong>Common-version signs:</strong> '+escape(r.data.common_signs.join(', '))+'</p>':'')+'<p><strong>Why it appeared:</strong> '+escape(r.data.clues.map(words).join(', ')||'Album search result')+'</p><p><strong>Check before buying:</strong> '+escape(r.data.verify.map(words).join('; '))+'</p><p class="small">'+escape(catalogCheck(r.data))+'</p><details><summary>Seller details and freshness</summary><p>Condition: '+escape(r.listing.condition||'Not provided')+'</p><p>Detail evidence checked '+escape(time(r.listing.details_observed_at||r.last_seen_at))+'</p>'+Object.entries(r.listing.item_specifics||{}).map(([k,v])=>'<div>'+escape(k)+': '+escape(Array.isArray(v)?v.join(', '):v)+'</div>').join('')+'</details></div><aside><span class="small">'+(r.listing.price_kind==='current_bid'?'Current bid + shipping':'Item + shipping')+'</span><div class="price">'+(r.data.subtotal?escape((r.data.currency||'')+' '+r.data.subtotal):'Total unknown')+'</div><p class="small">Tax / duties excluded</p><span class="badge '+(r.data.budget==='within_ceiling'?'':'amber')+'">'+escape(words(r.data.budget))+'</span>'+(stale||ended?'<p class="small">'+(ended?'Listing may have ended':'Needs a fresh check')+'</p><button class="quiet" data-recheck="'+i+'">Request fresh check</button>':'')+'<a href="'+escape(safeLink(r.listing.listing_url))+'" target="_blank" rel="noreferrer">Check listing on eBay ↗</a><div class="verdicts" role="group" aria-label="Your verdict">'+VERDICTS.map(([v,l])=>'<button class="quiet'+(r.verdict===v?' selected':'')+'" data-verdict="'+i+'|'+v+'">'+l+'</button>').join('')+'</div><details><summary>Ask the seller</summary><textarea readonly rows="5" class="message" id="message'+i+'">'+escape(sellerMessage(w))+'</textarea><button class="quiet" data-copy="'+i+'">Copy message</button></details><button class="quiet" data-dismiss="'+i+'">'+(r.dismissed?'Restore':'Dismiss')+'</button></aside></article>';}).join('')||'<div class="empty">'+(state.watches.some(w=>w.last_success_at)?'No unjudged leads meet these filters on this page. Try All prices for higher prices and older references, or Judged for listings you already reviewed.':'Your inbox will fill after the first scheduled scan.')+'</div>';
 document.querySelectorAll('[data-recheck]').forEach(b=>b.onclick=()=>action(async()=>{const r=rows[Number(b.dataset.recheck)];await api('/api/refresh-lead',{watch_id:r.watch_id,item_id:r.marketplace_item_id});notice('Fresh check queued. The next worker will retrieve current evidence.');}));
 document.querySelectorAll('[data-verdict]').forEach(b=>b.onclick=()=>action(async()=>{const [i,v]=b.dataset.verdict.split('|');const r=rows[Number(i)];await api('/api/verdict',{watch_id:r.watch_id,marketplace:r.marketplace,marketplace_item_id:r.marketplace_item_id,verdict:r.verdict===v?null:v});await load();}));
 document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=()=>action(async()=>{const text=$('#message'+b.dataset.copy).value;try{await navigator.clipboard.writeText(text);notice('Message copied. Paste it into “Contact seller” on eBay.');}catch{$('#message'+b.dataset.copy).select();notice('Select the message and copy it manually.');}}));
 document.querySelectorAll('[data-dismiss]').forEach(b=>b.onclick=()=>action(async()=>{const r=rows[Number(b.dataset.dismiss)];await api('/api/dismiss',{watch_id:r.watch_id,marketplace:r.marketplace,marketplace_item_id:r.marketplace_item_id,dismissed:!r.dismissed});await load();}));
}
let inboxCursor=null;
async function load(){try{state=await api('/api/dashboard?filter='+encodeURIComponent(filter)+'&prices='+encodeURIComponent(priceScope)+(inboxCursor?'&cursor='+encodeURIComponent(inboxCursor):''));notice('');render();}catch(e){if(e.status===401||e.status===503){$('#signin').hidden=false;$('#dashboard').hidden=true;$('#account').hidden=true;if(e.status===503)notice(e.message);}else throw e;}}
async function action(fn){try{await fn();}catch(e){notice(e.message);}}
function openEditor(id){editing=id||null;const form=$('#watchform');form.reset();const w=state.watches.find(w=>w.id===id);sheet={tells:(w?.config.tells||[]).map(c=>({...c})),anti_tells:(w?.config.anti_tells||[]).map(c=>({...c}))};renderSheet();if(w){for(const [key,value] of Object.entries(w.config)){if(form.elements[key]&&!Array.isArray(value))form.elements[key].value=value??'';}form.elements.condition.value=w.config.condition_ids.length===0?'any':w.config.condition_ids.includes('1000')?'new':'used';form.elements.auction_alert_minutes.value=String(w.config.auction_alert_minutes??120);form.elements.extra_queries.value=(w.config.extra_queries||[]).join('\n');}form.elements.release_id.readOnly=!!id;$('#editheading').textContent=id?'Edit watch':'Add a pressing';$('#editor').hidden=false;$('#editor').scrollIntoView({behavior:'smooth'});form.elements[id?'maximum_subtotal':'release_id'].focus();}
$('#prices').onchange=()=>action(async()=>{priceScope=$('#prices').value;inboxCursor=null;await load();});
$('#add').onclick=()=>openEditor();document.querySelectorAll('[data-addsign]').forEach(b=>b.onclick=()=>{const list=b.dataset.addsign;if(sheet[list].length>=12){notice('Up to 12 signs per list.');return;}sheet[list].push({kind:'keyword',value:'',required:false});renderSheet();});$('#cancel').onclick=()=>{$('#editor').hidden=true;};$('#refresh').onclick=()=>action(load);$('#nextpage').onclick=()=>action(async()=>{inboxCursor=state.next_cursor;await load();});$('#firstpage').onclick=()=>action(async()=>{inboxCursor=null;await load();});
$('#watchform').onsubmit=e=>{e.preventDefault();action(async()=>{const f=new FormData(e.target);const old=state.watches.find(w=>w.id===editing);const data={release_id:f.get('release_id'),label:f.get('label'),maximum_subtotal:f.get('maximum_subtotal'),currency:f.get('currency'),country:String(f.get('country')).toUpperCase(),postal_code:f.get('postal_code'),alert_mode:f.get('alert_mode'),gamble_max:f.get('gamble_max'),auction_alert_minutes:Number(f.get('auction_alert_minutes')),extra_queries:String(f.get('extra_queries')||'').split('\n').map(q=>q.trim()).filter(Boolean),tells:cleanSheet(sheet.tells),anti_tells:cleanSheet(sheet.anti_tells),condition_ids:f.get('condition')==='new'?['1000']:f.get('condition')==='used'?['2750','3000','4000','5000','6000']:[],enabled:old?old.config.enabled:true};await api('/api/watches'+(editing?'/'+editing:''),data,editing?'PUT':'POST');$('#editor').hidden=true;await load();notice('Watch saved. The next scheduled scan will apply your settings.');});};
document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;inboxCursor=null;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('selected',x===b));action(load);});
$('#login').onsubmit=e=>{e.preventDefault();action(async()=>{const email=$('#email').value;if(!codeSent){await api('/auth/send-code',{email});codeSent=true;$('#codefield').hidden=false;$('#code').required=true;$('#email').readOnly=true;$('#loginbutton').textContent='Verify and sign in';notice('Check your email for the sign-in code.');$('#code').focus();}else{await api('/auth/verify-code',{email,otp:$('#code').value});$('#code').value='';await load();}});};
$('#signout').onclick=()=>action(async()=>{await api('/auth/sign-out',{});location.reload();});
$('#notifications').onclick=()=>action(async()=>{const ios=/iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);if(ios&&!matchMedia('(display-mode: standalone)').matches&&!navigator.standalone)throw new Error('On iPhone, add Finder to your Home Screen from Safari, open that icon, then enable notifications.');if(!('serviceWorker' in navigator)||!('PushManager' in window))throw new Error('This browser does not support push here. Your review inbox still works.');if(!state?.push_key)throw new Error('Notifications are not configured yet.');const permission=await Notification.requestPermission();if(permission!=='granted')throw new Error('Notifications were not enabled. You can allow them in browser settings.');const registration=await navigator.serviceWorker.register('/sw.js');await navigator.serviceWorker.ready;let sub=await registration.pushManager.getSubscription();if(!sub){const key=state.push_key;const bytes=Uint8Array.from(atob(key.replace(/-/g,'+').replace(/_/g,'/')),c=>c.charCodeAt(0));sub=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:bytes});}await api('/api/push',sub.toJSON());$('#notifications').textContent='Notifications enabled';await load();notice('Notifications enabled for this device. Send a test notification below to check delivery.');});
$('#testpush').onclick=()=>action(async()=>{if(!('serviceWorker' in navigator))throw new Error('Enable notifications on a supported device first.');const reg=await navigator.serviceWorker.getRegistration();const sub=await reg?.pushManager.getSubscription();if(!sub)throw new Error('Enable notifications on this device first.');const result=await api('/api/push/test',{endpoint:sub.endpoint});notice(result.message);});
action(load);setInterval(()=>{if(state&&!document.hidden)action(load);},60000);
`;

export const serviceWorker = `self.addEventListener('push',event=>{let test=false;try{test=event.data?.json()?.kind==='test';}catch{}event.waitUntil(self.registration.showNotification(test?'Finder · Test notification':'Finder · New listing to check',{body:test?'Test received. Notifications can display on this device.':'A listing may be the pressing you want, at or under your price. Check the photos and signs in Finder.',tag:test?'finder-test':'finder-review-inbox',renotify:test,data:{url:'/'}}));});self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{const existing=list.find(c=>new URL(c.url).origin===self.location.origin);return existing?existing.focus():clients.openWindow('/');}));});`;
